import { Routes } from '@angular/router';
import { NunchakuManagerComponent } from './components/table-manager/table-manager.component';

export const routes: Routes = [
  { path: '', component: NunchakuManagerComponent }
];
